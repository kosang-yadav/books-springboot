create table book(
     id int not null ,
     title varchar(100) not null ,
     author varchar(100) not null ,
     publisher varchar(100),
     release_date varchar(20),
     isbn varchar(20),
     topic varchar(20)
);